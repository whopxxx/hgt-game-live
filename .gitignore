# ---- 运行时产物: 不入库 ----
# 日志 / 弹幕 / 题库都会随时间无限增长, 且含观众昵称, 不该进版本库。
#
# ⚠️ `data/*.jsonl` 只匹配**一层**, 不匹配 `data/quarantine_*/x.jsonl`。
# 而 quarantine 里装的恰恰是题库正文(旧 curated pool), 是最不该入库的一批。
# 所以下面必须再补一条 `data/**/` —— 用 `*` 会漏子目录。
data/*.log
data/*.jsonl
data/*.png
data/*.txt
data/**/*.jsonl
data/**/*.log
data/**/*.png
data/**/*.txt

# 导出/分析产物: 同样是运行时生成, 且含观众昵称。
# 注意 `data/提示词全文.md` 和 `data/最近20题.md` 是**手工维护**的
# 参考文档(受版本控制), 所以这里只按**具体文件名**忽略导出的那一份,
# 不能写 `data/*.md` —— 那会把上面两个也一起挡掉。
data/最近一次直播.md

# 临时调试脚本(我排查时随手写的, 用 _ 前缀)
data/_*.py

# Chrome 直播窗口的用户数据目录(160MB+)
data/_liveprofile/

# ---- 外部题库(Batch H) ----
# 抓下来的原始 dump + 归一后的 curated_raw。**绝不入库**:
#   1. 体量大(SE 抓一轮就是几 MB, 全量 dump 是 GB 级);
#   2. 里面是**别人的**受版权保护的内容, 入库等于我们再分发一份;
#   3. 它是可复现的 —— tools/import_*.py 重跑一次就有, 没有版本化价值。
# 真正需要版本化的是**导入脚本**与 attribution, 不是这些中间产物。
data_external/

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
