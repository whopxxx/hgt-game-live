# 海龟汤直播系统质量提升方案

> 项目：`whopxxx/hgt-game-live`  
> 审查基线：`main` 分支 commit `3d1ed85b9b46992ccd8738722114c8939e32b4a7`  
> 依据：当前 GitHub 代码、8 小时实测总结、最近 20 道实际播出题、当前全部提示词  
> 目标：优先提升“好不好玩”和“判得准不准”，其次才是降调用成本与缩短等待

---

## 0. 结论摘要

当前系统已经具备比较完整的工程骨架：状态机、强制工具调用、并发回答、重试、提示/揭晓、离线测试都已经存在。现在真正限制直播质量的，不是基础稳定性，而是下面四个结构性问题：

1. **出题只有“单题质量控制”，没有“整场内容分布控制”**  
   当前 `_too_similar()` 只用谜面 3-gram 做文本查重；`engine` 只把最近谜面文本传给下一题。它能抓“同一道题换个说法”，抓不住“职业换了，但解法骨架完全一样”。

2. **谜题生成 Prompt 存在强吸引子，持续奖励“职业 + 多年 + 怪规矩 + 悲情过去”**  
   `RIDDLE_SYSTEM` 同时要求短谜面、具体反常点、第三人称，又给出“职业怪癖”作为正向路线，模型自然收敛到最稳的模板。  
   更严重的是“谜面只给反常，不给线索”会鼓励模型把真正原因全部藏进题面外的私人往事，导致玩家无法推理，只能猜模型套路。

3. **通关裁判存在“规则很严、示例很松”的冲突**  
   `JUDGE_SYSTEM` 前面要求“说清关键机制”，后面的示例却把“他泡茶跟燃气管有关”判为 `true`。这实际上在教模型：**只要碰到谜底核心语义区域，就可以通关**。  
   这与最近 20 题里“纪念”“灯是为了引路吗”“因为害死过人”被判猜中高度一致。

4. **直播输入层仍然存在“用户发了，但系统无反馈”的情况**  
   `REVEALING / REVEALED / SETTING` 阶段的 `#问题` 会被 `_accept_danmaku()` 直接丢弃。  
   同时，当前重放识别是“1.5 秒内达到 3 条就整批丢”，在多人同时互动时有误杀正常弹幕的风险。

因此本轮不建议继续只微调 Prompt。建议按下面顺序推进：

**P0：裁判准确率 → P0：全局题型调度 → P0：公平线索/可推理性 → P1：非 QA 阶段反馈 + 重放识别 → P1：LLM 异常语义隔离 → P2：调用合并与预生成题池。**

---

# 1. 当前代码与问题的对应关系

## 1.1 当前出题链路

主要代码：

- `story/llm.py::PuzzleWriter.gen_riddle`
- `story/llm.py::_gen_once`
- `story/llm.py::_check_riddle`
- `story/llm.py::_too_similar`
- `story/engine.py::_enter_setting_locked`
- `story/engine.py::_tick_revealed_locked`

当前流程：

```text
最近谜面文本
    ↓
RIDDLE_SYSTEM + avoid 文本
    ↓
生成 puzzle / answer / hints
    ↓
CHECK_SYSTEM
    ↓
审稿人通过 / 就地修改
    ↓
3-gram 文本查重
    ↓
上屏
```

这个流程最大的问题是：**“不同”被定义成了文字不同，而不是推理机制不同。**

最近 20 题已经证明，下面这些题虽然文字差异很大：

- 邮递员
- 修表匠
- 灯塔守夜人
- 护士
- 豆浆铺老板
- 放映员
- 守林人

但玩家面对的其实是同一个问题：

```text
这个人为什么坚持一个怪习惯？
→ 以前是不是死过人？
→ 是不是亲人？
→ 是不是纪念 / 赎罪 / 弥补？
```

这就是“元游戏坍缩”：玩家不再解题，而是在猜模型。

---

## 1.2 当前查重只能抓文本重复，抓不到结构重复

`story/llm.py::_too_similar()` 使用字符 3-gram Jaccard，相同题改写能抓住，这个功能应该保留。

但它对下面两题几乎无能为力：

```text
修表匠每次修好表，都要倒扣一晚。
```

和：

```text
灯塔守夜人点灯后总要多留十五分钟。
```

文字几乎不重合，但如果谜底都是：

```text
很多年前发生死亡事故 → 现在做这个行为是纪念/赎罪
```

从游戏体验上它们就是高度重复。

另外，`story/llm.py` 中 `_too_similar()` 当前存在两份定义，应顺手删除重复实现，避免以后修改时只改到其中一份。

---

## 1.3 当前 reviewer 看不到真正的“连续几题”

`CHECK_SYSTEM` 写了：

```text
如果连续几道都是亲人去世 / 怀念亡者 / 赎罪，就换路线。
```

但 `_check_riddle()` 实际只传入：

```text
【谜面】
【谜底】
【提示】
```

审稿人没有最近题目的结构信息，因此它无法完成“连续几题”这个任务。

这类跨题约束不应该继续写在单题 reviewer 里，而应该放到独立的内容调度层。

---

## 1.4 “谜面不给线索”正在制造不可推理题

当前 `RIDDLE_SYSTEM` 中：

```text
谜面别把答案写出来。
谜面只给“反常”，不给“线索”。
```

第一句是对的，第二句应该删除。

好的海龟汤不是“不给线索”，而是：

> **不给答案，但必须给至少一个事后可回看的有效事实。**

最近题中多道题的真正原因来自题面完全没有提供的私人往事。玩家在正常问答过程中没有收敛路径，只能从模型常见套路中枚举：

```text
有人死了吗？
是亲人吗？
以前出过事故吗？
是在纪念吗？
是在赎罪吗？
```

这会同时造成：

- 题目越来越像；
- 观众越来越会“猜模型”；
- 裁判一旦宽判，单题瞬间结束；
- 提问数和真实题目质量失去相关性。

---

## 1.5 当前裁判的具体缺陷

代码：

- `story/llm.py::answer`
- `story/llm.py::judge`
- `JUDGE_SYSTEM`

当前逻辑是：

```text
emit_verdict
    ↓
如果不是开放疑问
    ↓
几乎每条都再调用 emit_judgement
    ↓
solved=True → 揭晓
```

这带来两个问题。

### 问题 A：第二次调用几乎每题都发生

只要：

- 有谜底；
- 不是 `_is_open_question()`；
- `judge_solve=True`；

就会额外调用一次 `judge()`。

这正好解释 8 小时实测里约 94% 的裁判调用没有实际作用。

### 问题 B：JUDGE prompt 自相矛盾

前面明确写：

```text
必须说清反常结果的原因；
必须说清关键机制；
需要裁判补一句才成立 → false。
```

但示例：

```text
“他泡茶跟燃气管有关” → true
```

实际上只说了“相关对象”，没有说清“为什么泡茶”“水汽如何起作用”。

这个例子应该是 `false`。

模型对具体 few-shot 示例的遵循往往比抽象规则更强，因此它很容易学成：

> 只要猜到谜底附近的主题，就算猜中。

这与实测中宽判现象高度吻合。

---

## 1.6 `_is_open_question()` 也存在过度拦截风险

当前 `_is_open_question()` 只要文本里出现：

```text
为什么 / 怎么 / 什么 / 哪个 / 多少 / ...
```

就直接认为不可能 solved。

它确实能拦住：

```text
他为什么跑？
```

但也会误伤这种包含明确假设的句子：

```text
为什么他每天多待十五分钟，是因为以前灯晚亮十五分钟出过事故吗？
```

这句话虽然包含“为什么”，但实际上已经提出完整因果假设，完全可能是正确答案。

因此这个硬规则应缩小用途，只过滤纯信息索取，不应该仅凭关键词决定是否允许通关。

---

# 2. 质量目标

下一轮不要只看“通过率”“调用次数”，建议建立四组核心指标。

## 2.1 内容多样性

最近 10 题目标：

| 指标 | 建议目标 |
|---|---:|
| 同一 `mechanism_family` | ≤ 2 题 |
| 死亡/丧亲类 | ≤ 2 题 |
| 怀念/赎罪/补偿类动机 | ≤ 2 题 |
| “多年职业 + 从不出错 + 怪规矩”骨架 | ≤ 1 题 |
| `repeated_ritual` 类 | ≤ 2 题 |
| 同一人物关系（亲人/伴侣等） | ≤ 3 题 |
| 明显同题改写 | 0 |

这不是要求每题随机，而是防止连续出现同一种解法。

---

## 2.2 可推理性

每道题必须满足：

1. 谜面至少存在 **1 个可回溯公平线索**；
2. 谜底核心机制不能完全依赖题面之外的私人历史；
3. 揭晓后能指出：
   - 哪个题面事实支持这个答案；
   - 为什么这个答案比普通替代解释更合理；
4. “有人死了 / 是纪念 / 是事故 / 是亲人”之类万能猜法不能直接通关。

---

## 2.3 通关判定

建立人工 golden set 后建议目标：

- `solved` **误判率优先压低**；
- 主题沾边不能通关；
- 完整机制猜中要稳定通关；
- “正确但换一种自然语言表达”不能被漏掉。

建议把通关从单个 boolean 改为覆盖度：

```text
cause_hit
mechanism_hit
key_fact_hit
```

最低通关要求：

```text
cause_hit == true
AND
mechanism_hit == true
```

`key_fact_hit` 可以作为增强项，而不是每题都强制。

---

## 2.4 直播体验

目标：

- 非 QA 阶段 `#问题` **0 条静默丢弃**；
- LLM 故障 **0 次伪装成“无关”**；
- 正常多人同时发言不能被重放算法整批误杀；
- 新题可做到基本无明显等待；
- 生成失败不能让直播内容质量下降到明显兜底题。

---

# 3. P0：先修通关裁判

这是当前最应该先改的部分，因为裁判误判会直接污染所有“题目是否容易”“平均提问数”“猜中率”数据。

---

## 3.1 立刻修 JUDGE_SYSTEM 的错误示例

把：

```text
“他泡茶跟燃气管有关”
→ true
```

改为：

```text
“他泡茶跟燃气管有关”
→ false
理由：只命中了相关对象，没有解释泡茶为什么有用。

“他泡茶产生水汽，是为了借水汽观察/判断燃气管是否泄漏”
→ true
理由：说清了反常行为、目的和机制。
```

同时增加几条与你实测高度相关的负例：

```text
“是在纪念某个人”
→ false

“以前是不是死过人”
→ false

“是不是以前出过事故”
→ false

“因为害死过人”
→ false
除非该句同时解释了当前反常行为为什么因此产生。

“灯是为了引路”
→ false
如果谜题真正反常点是“为什么只在退潮亮、涨潮灭”，
则必须解释潮汐与亮灭规则。
```

---

## 3.2 给每道题生成 `solve_atoms`

不要让 judge 每次重新从一段文学谜底里“凭感觉理解核心”。

建议扩展 `_TOOL_RIDDLE`：

```json
{
  "puzzle": "...",
  "answer": "...",
  "hints": ["...", "...", "..."],
  "solve_atoms": [
    "当前反常行为与某个过去事件有关",
    "过去事件造成了具体后果",
    "当前行为的机制/目的是什么"
  ]
}
```

更好的 `solve_atoms` 应该是题目真正需要玩家覆盖的原子事实，而不是简单摘词。

示例：

```text
题面：
守灯塔的人只在退潮亮灯，涨潮反而熄灯。

solve_atoms:
1. 灯光的目标不是单纯照亮航道，而是标出危险礁石。
2. 退潮时礁石接近/露出水面，灯光有意义。
3. 涨潮后继续亮灯反而可能造成错误引导。
```

玩家只说：

```text
“灯是引路的”
```

最多命中 0～1 个 atom，不能通关。

---

## 3.3 把 judgement 输出改成“覆盖结果”，不要只返回 bool

建议把 `_TOOL_JUDGE` 从：

```json
{
  "solved": true
}
```

改成：

```json
{
  "is_guess": true,
  "cause_hit": true,
  "mechanism_hit": false,
  "key_fact_hit": true,
  "matched_atoms": [0, 2],
  "solved": false
}
```

代码层最终决定：

```python
solved = cause_hit and mechanism_hit
```

这样能避免模型一句 `solved=true` 直接结束整题。

---

## 3.4 暂时不要马上删第二次 judge

虽然现在第二次调用浪费很大，但**第一阶段应先把正确率修好，再降调用**。

推荐两步：

### 阶段 A：先修准确率

仍保留 `answer → judge` 两段，但：

- 修正 JUDGE few-shot；
- 引入 `solve_atoms`；
- 记录 judge 覆盖结果；
- 建 golden set 回放。

### 阶段 B：准确率稳定后再降调用

给 `_TOOL_ANSWER` 增加：

```json
{
  "verdict": "是",
  "solution_candidate": false
}
```

只有：

```text
solution_candidate == true
或 verdict == 揭晓
```

才调用第二次 judge。

如果实测稳定，再进一步把 `solved` 和 atom coverage 直接合并进一次 ANSWER 调用。

这样比直接删除 judge 风险小得多。

---

# 4. P0：增加“全局题型调度器”

最重要的架构变化是：

> **不要让生成模型自己决定下一题属于什么类型。**

当前 `_gen_once()` 只收到：

```text
请出一个新的海龟汤谜题
+ 最近出过的谜面
+ 上一稿失败原因
```

建议改成：

```text
代码先选择下一题蓝图
    ↓
模型只负责执行蓝图
```

---

## 4.1 新增 `story/quality.py`

建议新增：

```python
@dataclass
class PuzzleBlueprint:
    mechanism_family: str
    emotion_mode: str
    domain: str
    time_shape: str
    relation: str

    death: bool
    past_trauma: bool
    long_term_profession: bool
    repeated_ritual: bool
```

候选值例如：

### `mechanism_family`

```text
功能错认
身份错认
信息差
规则系统
时间错认
空间错认
因果倒置
目标错认
观察者错认
物品用途错认
情感动机
```

### `emotion_mode`

```text
neutral
absurd
tense
warm
dark
```

### `time_shape`

```text
instant
same_day
recent
long_term
```

### `relation`

```text
stranger
coworker
friend
family
none
```

---

## 4.2 蓝图由代码选择，不增加 LLM 调用

不需要再找一个模型当“总导演”。

直接根据最近 10 题做 least-recently-used / quota 选择即可：

```python
def choose_blueprint(recent_signatures):
    # 1. 先排除近期超配额的机制
    # 2. 再从最久没出现的 mechanism_family 中选
    # 3. death / trauma / repeated_ritual 独立限额
    # 4. 确保与上一题至少 3 个维度不同
```

例如：

```text
mechanism_family = 信息差
domain = 商业
emotion_mode = neutral
time_shape = instant
relation = stranger
death = false
past_trauma = false
long_term_profession = false
repeated_ritual = false
```

模型必须按这个约束出题。

这会比“不要总写死亡”“换个完全不同的路子”可靠得多。

---

# 5. P0：新增 `PuzzleSignature`，做结构查重

生成完成后，每题保存：

```python
@dataclass
class PuzzleSignature:
    mechanism_family: str
    solution_shape: str
    hidden_motive: str
    past_event_type: str
    long_term_profession: bool
    repeated_ritual: bool
    death: bool
    relation: str
```

其中最关键的是：

```text
solution_shape
```

建议候选：

```text
past_trauma_explains_current_ritual
hidden_function_explains_odd_behavior
identity_reversal
rule_constraint
information_advantage
causal_reversal
time_reinterpretation
space_reinterpretation
goal_reversal
```

最近 10 题里：

```text
past_trauma_explains_current_ritual
```

如果已经出现 1～2 次，直接不允许再生成这一骨架。

---

## 5.1 保留现有 `_too_similar()`

现有 3-gram 查重仍然有价值，用于挡：

- 同题改写；
- 近似复述；
- 模型把旧题换几个词再输出。

新的结构查重是它的补充，不是替代。

最终建议：

```text
文本重复检测
+
signature / solution_shape 重复检测
+
配额控制
```

三层一起做。

---

# 6. P0：把“不给线索”改成“公平线索”

`RIDDLE_SYSTEM` 建议删除：

```text
谜面只给“反常”，不给“线索”。
```

替换成：

```text
谜面不能直接说出谜底，但必须包含至少一个“公平线索”：
知道谜底后重新读谜面，观众能指出“原来这个细节早就在暗示答案”。

禁止：
- 唯一答案完全依赖题面没有出现的私人往事；
- 谜底临时新增关键人物、关键身份、关键事故，题面完全没有对应抓手；
- 只有“作者说过去发生过某件事”才能解释反常行为。
```

---

## 6.1 工具 schema 增加 `fair_clues`

```json
{
  "fair_clues": [
    "谜面中的具体事实 A",
    "谜面中的具体事实 B"
  ]
}
```

reviewer 必须验证：

```text
fair_clues 里的内容是否真的出现在谜面？
这些事实是否确实支持谜底？
如果删除这些事实，题目是不是只剩作者脑补？
```

如果 `fair_clues` 无法成立，本题应直接重写，不要“就地小修”。

---

# 7. P0：重构 reviewer 的职责

当前 reviewer 的核心指令是：

```text
你的任务是改，不是退。
只在原稿上动该动的地方，其余一律保留原样。
```

这对“第一人称”“没问句”“提示泄露”这种局部错误很好。

但对：

```text
整题骨架就是悲情模板
题目没有公平线索
解法与最近 5 题一样
```

这种结构性问题，它反而阻止 reviewer 推倒重来。

---

## 7.1 reviewer 改成三种结果

建议 schema：

```json
{
  "decision": "pass | fix | rewrite",
  "issues": [
    "..."
  ],
  "puzzle": "...",
  "answer": "...",
  "hints": ["...", "...", "..."]
}
```

规则：

### `pass`

单题质量合格。

### `fix`

只适用于局部问题：

- 第一人称；
- 末尾没问句；
- 某句话泄底；
- 提示格式错误；
- 小范围因果表述不清。

### `rewrite`

用于结构问题：

- 没有公平线索；
- 谜底靠题面外私人故事硬补；
- 与最近题 `solution_shape` 重复；
- 万能猜法就能通关；
- 本身没有真正机关。

`rewrite` 后不要继续让 reviewer 在旧骨架上修，而是把原因交回 generator 重新出题。

---

# 8. P1：非 QA 阶段不能再“静默吞掉 #问题”

当前 `story/engine.py::_accept_danmaku()`：

```python
if self.phase != Phase.QA:
    ...
    return []
```

因此用户在：

```text
SETTING
REVEALING
REVEALED
```

阶段发送 `#问题`，虽然原弹幕可能进入弹幕轨道，但游戏没有任何回答。

建议不要把旧题问题带入下一题，因为会产生题目错配。

更合适的是 **0 成本确定性反馈**。

---

## 8.1 推荐行为

### `REVEALING`

```text
“本题正在揭晓，稍后开启下一题。”
```

### `REVEALED`

```text
“本题已结束，下一题即将开始。”
```

前端已有 `next_puzzle_ms`，可显示：

```text
“本题已结束，12 秒后下一题。”
```

### `SETTING`

```text
“正在准备新题，谜面出现后再发 #问题。”
```

这些都不需要 LLM。

---

## 8.2 代码实现建议

新增：

```python
def _phase_ack_locked(self, user_name, text, now):
    ...
```

输出一个 `QARec`：

```text
kind = "system"
```

或单独增加 `BROADCAST` notice。

不要把这些算进：

```text
stat_questions
verdict_counts
LLM transcript
```

这样既有反馈，又不会污染推理上下文。

---

# 9. P1：重做重放识别，禁止“纯流量阈值整批丢弃”

当前生产逻辑：

```text
1.5 秒窗口
达到 3 条
→ 判断疑似重放
→ 整批丢弃
→ 再压制 5 秒
```

这个假设：

```text
真人不可能 1.5 秒内连发 3 条指令
```

在单人直播间也许可接受，但当前已经有约 40 名发言观众。

多人看到关键提示或揭晓时，**1.5 秒内 3 条完全可能是真互动**。

因此当前算法随着直播变热，误伤概率会越来越高。

---

## 9.1 最优方案：使用平台消息唯一 ID

目前 `story/ingest.py::ChatEvent` 只有：

```text
user_id
user_name
content
ts
```

如果上游 protobuf 能拿到消息 ID，应把它一路传入：

```python
ChatEvent.message_id
DanmakuItem.message_id
```

然后直接做 ID 去重。

---

## 9.2 如果拿不到 message_id

不要再用“高密度 = 重放”。

改为：

```text
只有在真实发生 reconnect 后进入 replay_guard 窗口
+
新到消息 fingerprint 与最近已接收消息高度重复
```

fingerprint：

```python
(user_id, normalize(content))
```

建议流程：

```text
on_disconnect()
    ↓
标记 reconnecting

on_reconnect()
    ↓
开启 10~20 秒 replay_guard

guard 内：
    若连续 >=3 条都能在最近历史中找到完全匹配
        → 判定重放
    否则
        → 正常接收
```

这样能避免正常多人爆发被误杀。

---

## 9.3 必补测试

新增：

```text
3 个不同观众在 500ms 内各发一条不同问题
→ 不得判重放

10 个不同观众在 1 秒内同时发言
→ 不得判重放

重连后重复发送刚才完全相同的 8 条
→ 全部抑制

重连后有 6 条旧消息 + 2 条新消息
→ 旧消息抑制，新消息保留
```

---

# 10. P1：LLM 故障不能伪装成“无关”

当前存在两处语义污染：

### `director.py::_answer_work`

解析失败：

```python
results = [QAResult(... verdict="无关")]
```

### `engine.py::_tick_qa_locked`

重试耗尽：

```python
verdict="无关"
```

这会把：

```text
“模型没回答出来”
```

错误地告诉观众成：

```text
“你的推理与谜底无关”
```

这对游戏质量影响很大，因为它会主动把玩家带错方向。

---

## 10.1 增加“未判定”状态

建议增加：

```text
未判定
```

含义：

```text
系统暂时没能完成裁决，不代表你的猜测对或错。
```

显示文案：

```text
“刚才网络抖了一下，这条没判定，再发一次吧。”
```

该状态：

- 不进入 “是/不是/无关”统计；
- 不喂回 LLM transcript；
- 不触发 solved；
- 前端用独立灰色/橙色 badge。

---

# 11. P1：完善 LLM 空工具调用的重试策略

这里需要区分“已修复”和“仍可提升”。

## 当前仓库已经修掉的部分

`AnthropicMessagesClient._parse()` 当前已经能识别：

```text
tool_use 存在但 input={}
```

并能区分：

```text
输出触顶
工具调用空 input
```

这比 8 小时实测时描述的“空 input 没覆盖”已经更完整。

`_strip_puzzle_tail()` / `_looks_meta()` 也已经针对“提示混入谜面”增加了硬闸门和测试。

这些修复应该保留，并在下一轮直播验证是否真正归零。

---

## 仍然建议补的部分

`messages()` 当前对：

```text
HTTP 错误 / 网络错误
```

会 retry。

但 `_parse()` 返回：

```text
工具调用返回空 input
输出触顶
```

时，`messages()` 会把 `LLMResult(error=...)` 直接返回上层，并不会在客户端内部重试同一请求。

建议：

### 空 tool input

同一请求自动 retry 1～2 次。

### max_tokens 触顶

自动扩大一次预算：

```text
2500 → 3500
3000 → 4200
```

设置硬上限，防止无限增长。

### 回答类请求

如果最后仍失败：

```text
返回“未判定”
```

而不是“无关”。

---

# 12. P2：降低裁判调用成本

等 P0 的通关准确率通过回放验证后，再做性能优化。

推荐顺序：

## 方案 A：候选门控

第一次 `emit_verdict` 顺便返回：

```json
{
  "verdict": "是",
  "solution_candidate": false
}
```

只有 `solution_candidate=true` 时才调 judge。

这可以预计减少绝大多数第二次调用。

---

## 方案 B：最终合并成一次调用

长期可以把 schema 改成：

```json
{
  "verdict": "是",
  "comment": "方向对",
  "cause_hit": true,
  "mechanism_hit": false,
  "matched_atoms": [1],
  "solved": false
}
```

一次调用同时完成：

```text
普通裁决
+
通关判断
```

但一定要先通过离线 golden replay。

不要为了省 50% 调用，重新引入“猜到一个词就揭晓”。

---

# 13. P2：增加预生成题池

当前出题发生在 `SETTING` 阶段，实测平均约 12 秒，极端超过 60 秒。

这类延迟不应该继续暴露给直播观众。

建议增加容量 2～3 的：

```text
PuzzlePool
```

流程：

```text
当前题 QA 中
    ↓
后台低优先级准备未来题
    ↓
generator
    ↓
reviewer
    ↓
structure dedupe
    ↓
合格后进入 pool
```

揭晓结束：

```text
pool 非空
→ 立即拿下一题
→ 几乎无 SETTING 等待
```

---

## 13.1 防止预生成抢占回答资源

预生成不能影响“秒回”。

建议只在以下条件触发：

```text
当前 phase == QA
pending_count 很低
没有 hint / reveal 任务
题池不足目标容量
```

单独使用：

```python
ThreadPoolExecutor(max_workers=1)
```

并把它视为低优先级任务。

---

# 14. 建议的数据结构调整

## `story/state.py`

### `QAResult`

建议从：

```python
qid
verdict
comment
```

扩展为：

```python
qid
verdict
comment

status              # ok / unavailable
solution_candidate  # bool
cause_hit            # bool
mechanism_hit        # bool
matched_atoms        # list[int]
```

直播渲染不必显示这些内部字段，但日志必须保留。

---

## `RiddleResult`

建议增加：

```python
solve_atoms: list[str]
fair_clues: list[str]
blueprint: dict
signature: dict
review_notes: list[str]
```

---

## `RoundEngine`

当前 `_used_titles` 实际存的是谜面文本，建议重命名：

```python
_recent_puzzles
_recent_signatures
```

避免“变量名叫 title，实际存 puzzle”继续积累技术债。

---

# 15. 日志与评估体系

当前 `puzzle.jsonl` 已经会保存：

```text
puzzle
answer
reason
winner
reveal
qa
model
```

建议进一步加入：

```json
{
  "prompt_version": "v3",
  "quality_policy_version": "v1",
  "blueprint": {},
  "signature": {},
  "solve_atoms": [],
  "fair_clues": [],
  "generation_attempts": 2,
  "generation_latency_ms": 8200,
  "review_decision": "pass",
  "review_issues": [],
  "llm_calls": {
    "riddle": 1,
    "review": 1,
    "answer": 34,
    "judge": 4
  }
}
```

每条 QA 建议保存：

```json
{
  "text": "...",
  "verdict": "是",
  "solution_candidate": true,
  "cause_hit": true,
  "mechanism_hit": false,
  "solved": false,
  "latency_ms": 950,
  "judge_called": true,
  "status": "ok"
}
```

这样下一轮复盘就不再依赖从 1 万多行日志反推行为。

---

# 16. 建立离线 Golden Set

这是后续最重要的质量基础设施。

新增：

```text
tests/fixtures/judge_golden.jsonl
```

每条：

```json
{
  "puzzle": "...",
  "answer": "...",
  "solve_atoms": ["...", "..."],
  "user_text": "因为害死过人",
  "expected_solved": false,
  "expected_min_hits": 0
}
```

优先收录当前已经暴露出的边界样本：

```text
“纪念”
“有人死了吗”
“以前出过事故”
“因为害死过人”
“灯是为了引路吗”
“跟燃气管有关”
“他是某个职业”
```

以及完整正确答案。

发布新 Prompt 前跑整套回放，对比：

```text
false positive
false negative
judge call rate
```

---

# 17. 必增单测

## `tests/test_llm.py`

增加：

```text
test_judge_theme_only_is_false
test_judge_object_relation_only_is_false
test_judge_full_mechanism_is_true
test_judge_tragic_keyword_is_not_enough
test_open_question_with_explicit_hypothesis_can_solve
test_empty_tool_input_retries
test_llm_failure_returns_unavailable_not_irrelevant
```

---

## `tests/test_engine.py`

增加：

```text
test_revealing_question_gets_ack
test_revealed_question_gets_ack
test_setting_question_gets_ack

test_multi_user_burst_is_not_replay
test_reconnect_exact_replay_is_suppressed
test_reconnect_mixed_old_and_new_keeps_new
```

---

## 新增 `tests/test_quality.py`

覆盖：

```text
最近 10 题死亡类不能超过配额
同 solution_shape 不能连续出现
多年职业模板被限额
blueprint 至少多个维度与上一题不同
fair_clues 必须非空
```

---

# 18. Prompt 修改原则

## 出题 Prompt

从：

```text
“自由出一道好题”
```

改为：

```text
“执行给定 blueprint，设计一道满足机制约束的题”
```

不要再给“职业怪癖”这种特别容易收敛的单一正向出口。

---

## Reviewer Prompt

从：

```text
“尽量把当前稿修好”
```

改为：

```text
先判断问题属于局部错误还是结构错误。
局部错误 fix；
结构错误 rewrite。
```

---

## Judge Prompt

核心一句应是：

> **猜到题材、情绪、人物关系、过去有事故，都不等于猜到谜底。必须解释谜面中那个反常行为为什么发生。**

---

# 19. 推荐改造后的整体架构

```text
                  ┌──────────────────────┐
                  │  Recent Signatures   │
                  └──────────┬───────────┘
                             ↓
                  ┌──────────────────────┐
                  │ Blueprint Scheduler  │
                  │ 代码层配额/轮换       │
                  └──────────┬───────────┘
                             ↓
                  ┌──────────────────────┐
                  │   Riddle Generator   │
                  │ puzzle / answer      │
                  │ solve_atoms          │
                  │ fair_clues           │
                  │ signature            │
                  └──────────┬───────────┘
                             ↓
                  ┌──────────────────────┐
                  │   Quality Reviewer   │
                  │ pass / fix / rewrite │
                  └──────────┬───────────┘
                             ↓
              ┌──────────────────────────────┐
              │ deterministic hard gates     │
              │ meta / leak / duplicate /    │
              │ quota / structure            │
              └──────────────┬───────────────┘
                             ↓
                        Puzzle Pool
                             ↓
                            QA
                             ↓
                ┌────────────────────────┐
                │ verdict + candidate    │
                └────────────┬───────────┘
                             ↓
                       candidate?
                      /            \
                    no              yes
                    ↓                ↓
                 返回裁决          atom judge
                                     ↓
                              cause + mechanism
                                     ↓
                                   solved
```

---

# 20. 实施顺序

## 第一批：先让游戏“判得准”

1. 修 `JUDGE_SYSTEM` 矛盾示例；
2. 增加 `solve_atoms`；
3. judge 改成 coverage 输出；
4. 建 golden set；
5. 修 `_is_open_question()` 过度拦截；
6. LLM 故障不再返回“无关”。

**验收：**
“纪念 / 有人死 / 出过事故 / 跟 XX 有关”不再直接通关。

---

## 第二批：让题目“真的不一样”

1. 新增 `quality.py`；
2. 增加 `PuzzleBlueprint`；
3. 增加 `PuzzleSignature`；
4. 对 `solution_shape` 做最近 10 题配额；
5. 删除“谜面不给线索”；
6. 增加 `fair_clues`；
7. reviewer 支持 `rewrite`。

**验收：**
连续 10 题不再出现大量“多年职业 + 怪规矩 + 死人过去”。

---

## 第三批：让直播输入“有回应且不误杀”

1. 非 QA 阶段 `#问题` 给确定性 ACK；
2. 重放识别只在 reconnect guard 内工作；
3. 优先使用 message_id；
4. 无 message_id 时用历史 fingerprint 精确匹配；
5. 增加多人突发流量测试。

**验收：**
正常多人同时发问不整批消失；揭晓期发问不会石沉大海。

---

## 第四批：降成本和隐藏延迟

1. `solution_candidate` 门控第二次 judge；
2. golden replay 通过后考虑合并 ANSWER + JUDGE；
3. 增加 PuzzlePool；
4. 预生成只在回答压力低时执行。

**验收：**
judge 调用率明显下降，同时 solved 准确率不下降；下一题上屏几乎不需要现场等待。

---

# 21. 哪些当前代码应保留

以下设计是对的，不建议推倒：

- 强制工具调用拿结构化输出；
- `_unwrap_tool_input()` 处理网关套壳；
- `_parse()` 对 max_tokens / empty tool input 的显式识别；
- `_strip_puzzle_tail()` / `_looks_meta()`；
- `_is_first_person_story()`；
- `_has_closing_question()`；
- 提示重复检测；
- 现有 3-gram 同题查重；
- QA 并发池与 `qa_max_inflight`；
- Engine 使用纯状态机 + FakeClock 测试；
- 揭晓超时兜底；
- 题目落盘失败即停止推进；
- 生成失败有兜底而不是直播卡死。

下一轮的重点不是重写这些基础设施，而是在它们上面增加“内容导演层”和“明确通关条件”。

---

# 22. 最终建议

当前系统已经从“LLM 能不能稳定输出海龟汤”阶段，进入了下一个阶段：

> **如何把一个会生成题的模型，变成一个能持续主持数小时、不被观众摸清套路的游戏系统。**

这一阶段最关键的变化是：

```text
不要再让 LLM 自己管理全局质量。
```

LLM 适合：

- 写具体谜题；
- 改具体文案；
- 做语义判断。

代码应该负责：

- 题型轮换；
- 配额；
- 结构去重；
- 通关阈值；
- 状态机；
- 异常语义；
- 重放识别；
- 质量指标。

做到这个分工以后，下一轮直播的数据才会真正有分析价值。

否则即使继续微调 Prompt，系统仍然很容易进入：

```text
生成模板化
→ 玩家学会万能猜法
→ 裁判宽判
→ 单题快速结束
→ 数据看起来“猜中率很高”
→ 实际游戏性持续下降
```

本轮质量提升的第一目标，应当是切断这个闭环。
